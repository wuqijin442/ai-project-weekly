# 2026-07-27

## Hermes → dgx Ollama 配置（已完成）
- 目标：本机 Hermes-Agent 指向 dgx 上的 Ollama（deepseek + qwen）。
- dgx 地址确认：
  - 公网 `58.253.86.39:32222`（user taoyan，见 `~/.ssh/config` 的 `Host dgx`）
  - **内网 IP = `192.168.0.121`** —— 由 known_hosts 主机密钥比对确认：`[58.253.86.39]:32222` 与 `192.168.0.121` 的 ssh-rsa 完全一致（同一台机）；`192.168.0.120` 是另一台，密钥不同。
- 连接方式：**内网直连**（不走 SSH 隧道）：两份 config 的 `base_url` 与 `providers.ollama-launch.api` = `http://192.168.0.121:11434/v1`。
- 已设 `model.provider: ollama-launch`、`model.default: deepseek-r1:32b`、`api_key: ollama`，`providers.ollama-launch` 列出 `deepseek-r1:32b` + `qwen3-vl:30b`：
  - `C:/Users/Administrator/AppData/Local/hermes/config.yaml`（原生 Windows 主配置，已持久化）
  - `C:/Users/Administrator/.hermes/config.yaml`（被运行中的 Hermes 托管；运行实例里权威切换用 `/model ollama-launch:deepseek-r1:32b`）
- 前置：dgx 的 Ollama 须监听局域网（`OLLAMA_HOST=0.0.0.0:11434`），否则本机连不上。
- 验证：`curl http://192.168.0.121:11434/api/tags`；`ssh dgx "ollama list"`。
- 2026-07-27 实机验证：dgx 上 `curl http://192.168.0.121:11434/api/tags` 已返回模型列表（含 deepseek-r1:32b、qwen3-vl:30b、gemma4:31b、nomic-embed-text、taoyan-ess），确认 Ollama 监听局域网且 Hermes 配的地址可达。`OLLAMA_HOST=0.0.0.0:11434` 当时是 shell 内 export（仅当前会话），需写进 dgx 的 `~/.bashrc` 或 systemd env 才能开机持久。本机 Hermes 重启后 `/model ollama-launch:deepseek-r1:32b` 即可切换。

## GitHub 推送（08:33 追加，用户指令）
- 用户 08:33 提供 fine-grained PAT，要求直接用 token 推送。确认工作区已是 git 仓库，`origin` = `https://github.com/wuqijin442/ai-project-weekly.git`（分支 main）；原自动化记忆「工作区非 git 仓库无 remote」已过时。
- 改 `.gitignore`：移除 `Obsidian_Vault/`、`Logs/`、`*.log` 忽略（用户选择「推知识库并改 gitignore」长期纳入同步）。
- 提交并推送：stage 104 文件（Obsidian 知识库 Daily/Projects/_Index/.obsidian + 今日日志 logs/task_2026-07-27.log + .workbuddy/memory/2026-07-27.md + .gitignore），commit `[2026-07-27] Daily AI Project Update`，`git -c url."https://<TOKEN>@github.com/".insteadOf=... push origin main` 成功（8233f1f..a276f31）。远端已验证含 Vibe-Coding-2026-07-27.md / _Index.md / 今日日志 / .gitignore。
- 注：后续本自动化「GitHub 推送」步骤可正式启用（commit + token 内联 HTTPS 推送，复用远程 origin）。企业微信仍断开、跳过。
