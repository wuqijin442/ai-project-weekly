# 2026-07-20 工作记录

## 克隆并验证 2026-07-19 GitHub 周报 Top10
- 克隆目录：`cloned_projects/github-trending-2026-07-19/`
- 10 个仓库全部 `--depth 1` 克隆；3 个因 Windows MAX_PATH(260) checkout 失败 → `git config core.longpaths true` + `git checkout -f HEAD` 修复。
- **环境异常（重要）**：沙箱 git 出口对 `openinterpreter/openinterpreter` URL 返回 **OpenAI Codex** 仓库对象（含 codex-rs/，name=codex-monorepo），已复现（新建目录重克隆仍如此）。删除 2 个错误目录，最终 9 个正确克隆；openinterpreter 本环境无法克隆。
- 验证结论：
  - ✅ Graphify（pip install -e . + import graphify）、Vibe-Trading（pip install -e . + import api_server）、mattpocock/skills（npm install）、Nutlope/hallmark（npm install + serve 启动 :4173）、awesome-llm-apps（结构）、iOfficeAI/OfficeCLI（结构/Agent技能）。
  - ⚠️ OpenCut、openai/codex：克隆完整但本环境无 cargo/bun/bazel，**无法源码构建**。
  - ⏳ stablyai/orca：首次 npm install 因 ERESOLVE peer 冲突失败，已改用 `--legacy-peer-deps` 后台重装 + typecheck（结果待后台任务通知补充）。
- 报告：`logs/clone_verify_2026-07-20.md`
- 注意：Python 验证 venv 建在各仓库内 `.venv/`，可随仓库删除。

## 修复「需 cargo+bun 才能构建」问题（同日补充）
- 安装了缺失工具链，原 blocker 已解除：
  - **bun 1.3.14**：npm 安装因 npm 10.9.7 内部 bug 失败；改用 Node `fetch` 从 GitHub Release 下载 `bun-windows-x64.zip` + PowerShell `Expand-Archive` 解压 → `C:\Users\Administrator\.workbuddy\binaries\bun\bun.exe`。OpenCut web `bun run build` 成功（dist 2.1M）。
  - **Rust/cargo 1.97.1 (GNU)**：`winget install -e --id Rustlang.Rust.GNU` → `C:\Program Files\Rust stable GNU 1.97\bin\cargo.exe`。
  - **MinGW-w64 链接器** WinLibs POSIX.UCRT 16.1.0：`winget install -e --id BrechtSanders.WinLibs.POSIX.UCRT`（原 `BrechtSanders.MinGW-W64` 不存在 exit 20）。bin 在 WinGet Packages 目录的 `mingw64/bin`。
  - **bazel 不需要**：codex `codex-rs` 是 Cargo workspace，直接 `cargo build` 即可。
- 关键 PATH（每次新 shell 需重设，winget 改 PATH 需重启生效）：
  `export PATH="/c/Program Files/Rust stable GNU 1.97/bin:<WinLibs mingw64/bin>:$PATH"`
- OpenCut desktop（Tauri）与 codex-rs 两个 `cargo build` 已启动后台编译（200+/270+ crate 无错），二进制链接中；结果待后台通知补充到报告。
- orca 后台结果已回补：install ✅（1176 包），typecheck ❌ 系项目自身 strict-null TS 错误（TS18048），非环境缺陷。
- 可复用：此「Windows 沙箱补装 bun/rust/mingw」流程值得沉淀为 skill。
