# 2026-07-25 工作日志

## 自动化任务 automation-1784940038188 (TRAE WORK) — 从 BLOCKED 到 GitHub 同步完成
- 08:41 首次触发：脚本入口缺失（无 `/workspace`、无 main.py/board_workflow.py、非 git 仓库、GitHub 断连）→ 按"真实运行结果"规则未编造，出 BLOCKED 报告。
- 08:50 用户确认 GitHub 已连上并说"开始"。执行搭建：
  - 验证网络：github.com 可达、HTTPS git clone 实测成功。
  - `git init` + `.gitignore`（排除 cloned_projects/、clones/、node_modules/ 等）+ 10 个输出目录。
  - 编写真实 `main.py`（标准库+git）：Trending 抓取→AI 过滤→TOP5→真实 Clone/安装/冒烟→中文日报+Metadata→本地提交+推送。
  - 修复 4 个真 bug：clone 路径空格、main 分支名、shell=True 超时失效、Trending 正则。
- 真实运行结果：扫描 16 / 过滤 9 / Clone 5 / 安装成功 2 / 运行 0 / 推荐 0。日报 `Daily-Reports/2026-07-25.md` 已生成，本地提交 5 次（分支 main）。
- **10:09 GitHub 同步成功**：用户创建 `wuqijin442/ai-project-weekly` 后，使用本机已有 SSH key（id_rsa）通过 SSH 推送到 `main`，合并了远程 initial README 并保留本地 README。仓库公开地址：https://github.com/wuqijin442/ai-project-weekly
- `main.py` 已调整为默认 SSH remote、保留现有 origin、支持可选 `GITHUB_TOKEN` HTTPS 模式。
- 产出文件：main.py、README.md、Daily-Reports/2026-07-25.md、Metadata/2026-07-25.json、各输出目录、自动化 memory.md。
- 待补：board_workflow.py（11 板块测试）。
- 10:16 补充：用户要求"忽略上传的内容"。确认 `.uploads/` 已在 .gitignore；发现 `.workbuddy/tmp/`（抓取脚本/JSON/缓存）与 `.workbuddy/memory`、`automations` 内部记忆被 `git add -A` 误带入公开仓库，已将 `.workbuddy/` 整体加入 .gitignore 并 `git rm --cached` 取消跟踪后推送（本地文件保留）。公开仓库现仅含交付物（main.py/README/Daily-Reports/Metadata/各输出目录/.gitkeep/logs）。注：`logs/` 仍按原任务规范同步，其中 `logs/wecom_message_2026-07-05.txt` 可能含企业微信消息，如不想公开可一并忽略。

## 目录树按职能物理重组（10:26–10:34）
- 用户要求"重新对文件的树形结构进行分类" → 选"物理重组目录"。
- 重组为 5 类：src（main.py 入口）、reports（daily/weekly/monthly/boards）、knowledge-base（projects/awesome）、analysis（reviews/benchmarks/architecture）、data（metadata/screenshots）；保留 logs/。
- 坑：NTFS 大小写不敏感（Knowledge-Base==knowledge-base）。先用 `git mv` 误嵌套，git reset --hard 恢复；后改文件系统 mv + git add -A，遇空 .gitkeep 内容相同导致 status 中 rename 显示错位——但 `git write-tree` 验证实际提交树路径 100% 正确，可放心。
- main.py：ROOT 改为 `Path(__file__).resolve().parent.parent`；DAILY_DIR→reports/daily、META_DIR→data/metadata；python 验证解析正确。
- README 同步新结构；恢复 `.workbuddy/` 全量忽略（此前被改仅忽略 tmp/automations，会泄漏个人 memory）。
- 提交 36dda12 并推送 main（HTTPS 本次也成功，SSH 仍可用）。公开仓库结构已更新。

## 实现 board_workflow.py（11 板块 × TOP5 真实测试，10:43–10:57）
- 用户选"2"=实现 board_workflow.py。复用 main.py 的真实运行函数（clone/install/smoke/score/sync），import 不触发 main()（有 `__main__` 守卫）。
- 板块抓取原写 GitHub topic HTML 页，实测结构无 `Box-row`、且 topic 页不含 star 链接 → 0 结果。改走 **GitHub Search API**（`/search/repositories?q=topic:<slug>+stars:>50&sort=stars`）抓取真实仓库（star/语言/简介齐全），无需 token。
- 规避未认证 Search API 10 次/分钟限速：板间 `BOARD_API_PACE`(默认7s) 间隔 + 403 按 `X-RateLimit-Reset` 精确退避重试。
- 有界真实验证：限定 1 板块(rag)、每板前2项目、跳过 git，实跑 fetch→clone(dify 70.7s/open-webui 18.3s)→install/smoke→report，报告结构正确；安装因重仓超 90s 测试超时记为 failed（真实结果，非 bug）。
- 清理测试产物（报告/metadata/clones/boards），仅提交脚本+README：532eaf4 已推送 main。完整 11×5 运行耗时长，留待自动化/手动触发。
