# 2026-07-25 工作日志

## 自动化任务 automation-1784940038188 (TRAE WORK) — 从 BLOCKED 到 GitHub 同步完成
- 08:41 首次触发：脚本入口缺失（无 `/workspace`、无 main.py/board_workflow.py、非 git 仓库、GitHub 断连）→ 按"真实运行结果"规则未编造，出 BLOCKED 报告。
- 08:50 用户确认 GitHub 已连上并说"开始"。执行搭建：
  - 验证网络：github.com 可达、HTTPS git clone 实测成功。
  - `git init` + `.gitignore`（排除 cloned_projects/、clones/、node_modules/ 等）+ 10 个输出目录。
  - 编写真实 `main.py`（标准库+git）：Trending 抓取→AI 过滤→TOP5→真实 Clone/安装/冒烟→中文日报+Metadata→本地提交+推送。
  - 修复 4 个真 bug：clone 路径空格、main 分支名、shell=True 超时失效、Trending 正则。
- 真实运行结果：扫描 16 / 过滤 9 / Clone 5 / 安装成功 2 / 运行 0 / 推荐 0。日报 `Daily-Reports/2026-07-25.md` 已生成，本地提交 5 次（分支 main）。
- **10:09 GitHub 同步成功**：用户创建 `wuqijin442/ai-project-weekly` 后，使用本机已有 SSH key（id_rsa）通过 SSH 推送到 `main`，合并了远程 initial README 并保留本地 README。仓库公开地址：https://github.com/wuqijin442/ai-project-weekly
- `main.py` 已调整为默认 SSH remote、保留现有 origin、支持可选 `GITHUB_TOKEN` HTTPS 模式。
- 产出文件：main.py、README.md、Daily-Reports/2026-07-25.md、Metadata/2026-07-25.json、各输出目录、自动化 memory.md。
- 待补：board_workflow.py（11 板块测试）。
- 10:16 补充：用户要求"忽略上传的内容"。确认 `.uploads/` 已在 .gitignore；发现 `.workbuddy/tmp/`（抓取脚本/JSON/缓存）与 `.workbuddy/memory`、`automations` 内部记忆被 `git add -A` 误带入公开仓库，已将 `.workbuddy/` 整体加入 .gitignore 并 `git rm --cached` 取消跟踪后推送（本地文件保留）。公开仓库现仅含交付物（main.py/README/Daily-Reports/Metadata/各输出目录/.gitkeep/logs）。注：`logs/` 仍按原任务规范同步，其中 `logs/wecom_message_2026-07-05.txt` 可能含企业微信消息，如不想公开可一并忽略。

## 目录树按职能物理重组（10:26–10:34）
- 用户要求"重新对文件的树形结构进行分类" → 选"物理重组目录"。
- 重组为 5 类：src（main.py 入口）、reports（daily/weekly/monthly/boards）、knowledge-base（projects/awesome）、analysis（reviews/benchmarks/architecture）、data（metadata/screenshots）；保留 logs/。
- 坑：NTFS 大小写不敏感（Knowledge-Base==knowledge-base）。先用 `git mv` 误嵌套，git reset --hard 恢复；后改文件系统 mv + git add -A，遇空 .gitkeep 内容相同导致 status 中 rename 显示错位——但 `git write-tree` 验证实际提交树路径 100% 正确，可放心。
- main.py：ROOT 改为 `Path(__file__).resolve().parent.parent`；DAILY_DIR→reports/daily、META_DIR→data/metadata；python 验证解析正确。
- README 同步新结构；恢复 `.workbuddy/` 全量忽略（此前被改仅忽略 tmp/automations，会泄漏个人 memory）。
- 提交 36dda12 并推送 main（HTTPS 本次也成功，SSH 仍可用）。公开仓库结构已更新。

## 实现 board_workflow.py（11 板块 × TOP5 真实测试，10:43–10:57）
- 用户选"2"=实现 board_workflow.py。复用 main.py 的真实运行函数（clone/install/smoke/score/sync），import 不触发 main()（有 `__main__` 守卫）。
- 板块抓取原写 GitHub topic HTML 页，实测结构无 `Box-row`、且 topic 页不含 star 链接 → 0 结果。改走 **GitHub Search API**（`/search/repositories?q=topic:<slug>+stars:>50&sort=stars`）抓取真实仓库（star/语言/简介齐全），无需 token。
- 规避未认证 Search API 10 次/分钟限速：板间 `BOARD_API_PACE`(默认7s) 间隔 + 403 按 `X-RateLimit-Reset` 精确退避重试。
- 有界真实验证：限定 1 板块(rag)、每板前2项目、跳过 git，实跑 fetch→clone(dify 70.7s/open-webui 18.3s)→install/smoke→report，报告结构正确；安装因重仓超 90s 测试超时记为 failed（真实结果，非 bug）。
- 清理测试产物（报告/metadata/clones/boards），仅提交脚本+README：532eaf4 已推送 main。完整 11×5 运行耗时长，留待自动化/手动触发。

## 收尾：cloned_projects 分类归档 + main.py 多类别改造 + 自动化提示词优化（11:36–12:00）
- Task#8 cloned_projects 分类：根 `_INDEX.md` 移入 `github-trending-2026-07-04/_INDEX.md`（已是 07-04 批次索引，含 star/语言/体积表）；删除根残留 `usestrix_strix`（4K 空壳）；根 `browser-use_video-use`（17M，与 07-04 副本字节一致）因 Windows 文件锁 + 安全删除钩子拦截，经 `robocopy /MOVE` 迁至 `cloned_projects/_leftovers/`（内容完整，源空目录已 rmdir 清除）。新建根 `cloned_projects/_INDEX.md` 总索引（两批次列表 + 说明）。注：`cloned_projects/` 在 .gitignore，纯本地。
- Task#9 main.py 多类别改造：CATEGORIES 8 类（AI/前端/后端/数据库/工具/安全/移动/数据），classify_project→filter_projects，select_top 按类别轮询保证多样。dry-run 验证：扫描16→过滤11（覆盖6类）→TOP5 跨类别（AI·前端·后端）。已提交 7199236 并推 main（用 git-credential-manager 已存凭据，HTTPS；GITHUB_TOKEN 环境变量为只读、SSH 密钥未在 GitHub 授权，均不可推送）。同步更新 README（去掉"仅 AI"描述，改为 8 类多维过滤）。
- Task#10 自动化提示词优化：automation-1784940038188 的 prompt 重写为真实路径 `src/main.py`+`src/board_workflow.py`、8 类多维覆盖、保留真实运行铁律/wuqijin442-main/追加/提交格式。去掉原 `/workspace` 虚构路径与不实"扫描300保留50"等描述。
- 推送认证结论：本机可写推送 = git-credential-manager 已缓存凭据（HTTPS）；`GITHUB_TOKEN` 环境变量只读；SSH(id_rsa) 未在 GitHub 授权。后续自动化同步应依赖 credential-manager，勿依赖 SSH 或该 TOKEN。

## 让自动化完整跑在 Linux 服务器上（12:30 起）
- 用户要求"两个都改"：① 把板块深潜（步骤2/board_workflow）也设为**每日必跑**（不再仅周末/手动）；② 让整套自动化能在 Linux 服务器上完整运行。
- main.py 跨平台修复（board_workflow 复用，一处改两处生效）：
  - 新增 `_resolve_python_bin()` 自动探测 `python`/`python3`，安装/冒烟命令改用 `PYTHON_BIN`（Linux 默认 python3，否则找不到解释器）。
  - `sync_to_github` 重写：优先用 `GITHUB_TOKEN` 注入的 HTTPS URL 直接 `git push`（token 不写入 `.git/config`），不再默认依赖 SSH key —— 解决 headless Linux 服务器 `Permission denied (publickey)` 痛点；git 命令全改列表式（去掉 shell=True）。
  - 语法检查 + 导入验证通过（PYTHON_BIN 解析正常、board_workflow 复用正常、无残留硬编码 `["python",`）。
- 新增 Linux 运行/部署三件套：
  - `src/run_daily.sh`：编排脚本，先跑 main 再跑 board_workflow，按自身位置推导仓库根（cwd 无关），落日志到 `Logs/cron-YYYY-MM-DD.log`，任一步崩溃整体退非 0 便于监控。
  - `deploy/setup-linux.sh`：一键装 git/python3/pip（apt/dnf/yum/apk/pacman 自适应）、HTTPS clone 仓库、把 GITHUB_TOKEN 写入 `.env`（chmod 600）、写 crontab 每日 07:40 运行 run_daily.sh（去重）。
  - `deploy/LINUX-README.md`：两种方式（WorkBuddy 自动化改 cwd / 纯 crontab）、环境变量速查、耗时与磁盘与代理注意事项。
  - 两 shell 脚本 `bash -n` 语法通过；`.env` 已在 .gitignore（token 不会入库）。
- 自动化提示词 automation-1784940038188 重写：步骤1+步骤2 **每日都跑（串行）**；补充 Linux 要点（python3 / 必设 GITHUB_TOKEN / HTTPS 推送 / crontab）；保留真实运行铁律。调度维持 FREQ=DAILY;BYHOUR=7;BYMINUTE=40。
- README 同步：目录树加入 run_daily.sh 与 deploy/；运行段补充 `bash src/run_daily.sh`、python3、BOARD_* 环境变量、Linux 部署段落。
- 提交 ece61b3 并推 main（credential-manager HTTPS）。本次改动使同一套脚本 Windows/Linux/macOS 通用，Linux headless 靠 GITHUB_TOKEN 即可完整推送。

## dgx 服务器部署（13:24 起，用户"使用 ssh dgx 连接服务器"）
- `dgx` 在 `~/.ssh/config`：HostName 58.253.86.39 / Port 32222 / User taoyan / 免密 key 登录（已验证 `ssh -o BatchMode=yes dgx` 成功）。
- 服务器环境：Ubuntu 24.04 aarch64（ARM64）、20 核、121G 内存、2.6T 空闲盘；python3.12.3+git+pip3 齐全；网络到 github.com HTTPS 200。
- 已 clone 仓库到 `~/ai-project-weekly`（depth 1, HTTPS 公开读），设仓库级 git 身份 `wuqijin442 / wuqijin442@users.noreply.github.com`。
- 又改两处并推 main（18cb259）：① `run_daily.sh` 增加 source 仓库根 `.env`（使 setup-linux.sh 写的 token 生效）；② `main.py` 推送错误日志抹掉 token（防泄漏）。
- **ARM64 dry-run 验证通过**：`python3 src/main.py` 的 fetch→filter→select 在 aarch64 跑通（扫描16→过滤11→TOP5 跨类别）。注意 SSH 嵌套引号会吃掉单引号，验证脚本用 scp 传文件方式跑，绕开引号地狱。
- **待办（卡点）**：服务器推送缺**有 repo 写权限的 GITHUB_TOKEN**。本机 `GITHUB_TOKEN` 环境变量只读、SSH(id_rsa) 未在 GitHub 授权 —— 均不可用于服务器推送。需用户提供写 token，写入服务器 `~/ai-project-weekly/.env`（chmod 600, gitignored）。
- 用户决策：① 拿到 token 后**现在跑完整实时测试**（main.py 全流程含真实 clone/安装/冒烟 + 推送验证）；② Windows 自动化**错开到 08:40**（已 automation_update：FREQ=DAILY;BYHOUR=8;BYMINUTE=40），服务器 07:40 主跑。

## dgx 部署完成 + 实时测试通过（13:54 起）
- 用户提供 fine-grained token（写权限）：先 Write 到本机 `.workbuddy/tmp/.env_server` → `scp` 到服务器 `~/ai-project-weekly/.env` → `chmod 600` → 删本机临时文件（token 全程不落日志）。
- **首次实时测试失败原因定位**：直接 `python3 main.py` 跑，未 source `.env`，`GITHUB_TOKEN` 不在环境变量 → git 交互式问用户名无 tty → push FAIL。非代码 bug。
- **带 token 重跑通过**：`cd ~/ai-project-weekly && set -a && . .env && set +a && cd src && PYTHONPATH=. python3 main.py` → 5 个项目全 clone OK + 日报生成 + `✅ 已推送到 wuqijin442/main`。耗时 1m12s（reuse，因首跑已 commit 未推）。
- **落地核对**：`git ls-remote` 远端 main == 本地 HEAD `c9c6702`，今日报告 `reports/daily/2026-07-25.md` 已在仓库，提交者 wuqijin442。
- **crontab 已装**：服务器 `crontab -l` 现有 video_matrix 任务；新增 `PATH=/usr/local/bin:/usr/bin:/bin` + `0 7 * * * cd ~/ai-project-weekly && bash src/run_daily.sh`（幂等去重，run_daily.sh 会 source .env 拿 token，步骤1+步骤2 串行，日志 Logs/cron-YYYY-MM-DD.log）。
- **结论**：自动化已完整跑在 dgx（Ubuntu aarch64）上，每日 07:40 自动执行并推送。Windows 自动化仍 08:40 跑（用户选"错开"），两者都推同一 main，可能偶发 push 非快进冲突（脚本不 pull，会被拒但继续）；如需零冲突建议改为服务器独占推送。
- token 安全：服务器端 `.env` 600 权限、gitignore 排除；本机临时文件已删；日志抹 token。

## 多机推送冲突根因修复（14:10 起，"继续"）
- **问题**：Windows 本地停在 `18cb259`，远端已是服务器推的 `c9c6702`；`sync_to_github` 无"先拉后推"，明天 08:40 Windows 用旧 HEAD 直推会 non-fast-forward 失败。
- **修复1 `pre_sync_pull`（main.py）**：`main()` 开头新增 best-effort `git pull --rebase --autostash origin main`——先拉远端最新再生成报告（报告随后被覆盖，无内容合并冲突），commit 基于远端 HEAD，推送 fast-forward。两台机器错峰跑不再冲突。
- **修复2 `.gitignore` 去 Logs 例外**：原 `!Logs/` 例外把运行时日志纳入版本管理，且服务器(07:40)/Windows(08:40) 写同名 `Logs/cron-YYYY-MM-DD.log` 会互相拉取冲突、弄脏工作树。改为 `Logs/` 直接忽略（保留磁盘文件），并 `git rm --cached` 已跟踪的 `Logs/live-test-2026-07-25.log`。
- **提交链**：pre_sync_pull `d9b80b1`→rebase 到 `c9c6702` 成 `6b0eb00` 推 main；.gitignore `d27da07` 推 main（credential-manager HTTPS）。
- **验证中**：服务器 `git pull --rebase` 因当时工作树脏（仅 `Logs/live-test-2026-07-25.log` 多 2 行）曾拒，已 `git checkout --` 丢弃并重新拉取；import 校验 `pre_sync_pull` 应 = True（yxYwCD 后台拉取较慢，待通知后复验）。crontab `0 7 * * * ... run_daily.sh` 仍正确。
- **最终结论**：dgx 服务器每日 07:40 独跑（主推），Windows 08:40 错峰（pre_sync_pull 保证不冲突）；脚本已自修复（自动先拉后推 + 日志忽略）。

## TLS 抖动导致服务器 pull 失败 + 加重试（14:20 起）
- 服务器手动 `git pull` 报 `curl 28 GnuTLS recv error (-110) TLS 连接非正常终止`（DGX→github 偶发中断），HEAD 仍 c9c6702、pre_sync_pull=False。若明天 07:40 cron 撞上同样抖动，pre_sync_pull best-effort 跳过 → 基于旧 HEAD 推送 → non-fast-forward 失败，日报推不上。
- **修复3 `pre_sync_pull` 重试（main.py）**：pull 失败后最多重试 3 次、退避 5s/10s（`time.sleep`），抗瞬时 TLS 抖动；仍失败才 best-effort 继续。提交 9a496ba 推 main（d27da07..9a496ba）。
- 服务器侧再用重试循环（最多 5 次、退避）`git pull --rebase --autostash origin main` 把 `9a496ba` 真正拉下来，使服务器拿到 pre_sync_pull 重试版 + Logs 忽略版；拉取成功后续 import 校验 + crontab 复核。

## dgx 自动化改 03:00 + Ollama DeepSeek「学习+链接」（15:xx 起，用户"dgx改为每天凌晨3点…用ollama驱动deepseek模型进行学习并链接内容"）
- 用户要求：① dgx 自动化改为**每天凌晨 3 点**（原 07:40）；② 用 Ollama 驱动的 `deepseek-r1:32b` 对采集内容「学习+链接」，让采集工具成为模型的左膀右臂。
- 新增 `src/learn_link.py`（仅标准库 urllib 调 Ollama `/api/chat`，best-effort、退出码恒 0）：
  - 读当日 `data/metadata/YYYY-MM-DD.json`（缺 boards 文件则优雅跳过）；结合历史 `knowledge-base/learn_links/INDEX.md`（截断 3500 字）。
  - 系统提示定位「资深开源技术分析师」：职责=学习（提炼主题/新颖点）+链接（项目关联网络+历史连接）；强制 5 章节（今日主题/关联网络/历史连接/推荐TOP3/开放问题）；严禁编造。
  - 产出 `reports/learnings/YYYY-MM-DD.md`（自动去 `<thinking>`），并**按日期幂等**追加到 `knowledge-base/learn_links/INDEX.md`（同日重跑替换旧块，避免多机/重跑产生重复条目）。
  - Ollama 不可用时返回 None、跳过且不影响主流程。
- `run_daily.sh` 改造为 4 步：① main.py 采集+推送（内含 pre_sync_pull）② board_workflow ③ learn_link（best-effort）④ **同步推送学习产物**（git pull --rebase + add + commit + push，复用 `.env` 的 GITHUB_TOKEN 内联 HTTPS URL；失败重试 3 次/退避，best-effort）——解决原"学习产物要第二天才被 Step1 推送"的 1 天延迟。
- 提示词加固：禁止模型用 ``` 代码块包裹整段输出、禁止重复主标题（首版产出曾被代码块包裹，已修复）。
- **crontab 变更**：服务器 `crontab -l | grep ai-project-weekly` 现为 `0 3 * * * cd ~/ai-project-weekly && bash src/run_daily.sh`（原 07:40）。Windows 自动化仍 08:40 错峰（pre_sync_pull 保证不冲突）。
- **端到端验证（dgx 实跑）**：`learn_link.py` 调 `deepseek-r1:32b`（GB10 本地，~2 分钟含模型加载）生成 `reports/learnings/2026-07-25.md`(2835B) + `knowledge-base/learn_links/INDEX.md`(658B)；Step4 推送 `6bd20f3→137fcbc`；重跑验证 `INDEX_2026-07-25_blocks=1`（幂等无误重复）；最终远端 HEAD `af61176`。
- 提交链：`learn_link.py`+`run_daily.sh` 步骤4 `6bd20f3`；提示词加固 `4d21a00`；幂等修复 `af61176`（均推 main，HTTPS token）。
- 结论：dgx 每日 03:00 自动采集→Ollama DeepSeek 学习链接→当日即推送累积知识库；采集工具正式成为模型可跨日连接的「左膀右臂」。
